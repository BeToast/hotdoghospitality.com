import { categories } from "../Home";
import ImageCard from "./ImageCard";

interface Category {
   id: string;
   label: string;
   src: string;
}

const About = () => {
   return (
      
   );
};

export default About;
